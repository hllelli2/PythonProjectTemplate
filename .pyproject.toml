[build-system]
requires = ["setuptools>=65.5.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = ""
version = "0.0.0"
description = "Your project description here"
authors = [
    { name = "Your Name", email = "you@example.com" }
]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = [
    # dependencies go here
]

[tool.uv]
# optional: uv configuration if needed
[tool.uv.group.dev]
dependencies = [
    "pytest",
    "pytest-cov",
    "ruff",
    "mypy",
    "dapperdata",
    "tombi",
    "build",
]

[tool.uv.group.lock]
# optional: lock group configuration if needed
